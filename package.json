{
  "name": "my-first-mentraos-app",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "bun run dist/index.js",
    "dev": "bun --watch src/index.ts",
    "gemini-test": "bun --watch src/gemini-button-test.ts",
    "transcription-test": "bun --watch src/transcription-test.ts",
    "audio-test": "bun --watch src/audio-transcription-test.ts"
  },
  "dependencies": {
    "@mentra/sdk": "latest",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "@types/express": "^4.17.21"
  }
}
